server:
  url: "ws://your-panel-host:8080/api/agent/ws"
  token: "same-token-as-server-agent-token"

node:
  id: "oracle-sg-01"
  name: "Oracle SG"

runtime:
  interval: "5s"
  mode: "normal"

features:
  docker: false
  terminal: false
